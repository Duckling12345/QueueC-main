import 'package:flutter/material.dart';

class FormsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('Forms', style: TextStyle(fontSize: 60)),
        ),
      );
}
