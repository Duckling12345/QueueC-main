import 'package:flutter/material.dart';

class ParkingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('Parking', style: TextStyle(fontSize: 60)),
        ),
      );
}
